/*globals define*/

define(function (require) {
  "use strict";

  var map, $;

  require("css!../css/common.css");
  $   = require("jquery");
  map = require("map");

  $(".view-loader").hide();
});
